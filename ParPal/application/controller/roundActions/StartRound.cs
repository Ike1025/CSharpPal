class StartRound(string keyword, RoundManager roundManager) : Request {
    public string Keyword {get;} = keyword;
    RoundManager roundManager = roundManager;
    public void Execute() {
        
    }
}